# HTML 动效功能说明

## 创建 HTML 动效

1. 在编辑器中插入 HTML 元素。
2. 选中该元素，在右侧面板分别填写 HTML、CSS 和 JavaScript。
3. 拖动、缩放或旋转元素，把它放到 PPT 的合适位置。
4. 进入放映查看交互效果。

HTML 代码运行在独立的 sandbox iframe 中，不会直接访问 PPTist 页面或工程状态。HTML 元素默认透明，适合叠加在文字、图片和背景之上。

## 内置模板

### 转盘

选择转盘后，可以在右侧添加、修改和删除选项，设置旋转时长、是否显示结果以及抽中后是否移除选项。点击转盘中心或转盘本身开始旋转。

### 电影胶卷

上传多张图片后，照片会组成一条连续移动的胶卷带自动播放，不提供额外的播放、暂停和翻页按钮。可以设置是否循环和胶卷移动一周的时长。

### 粒子照片秀

支持雨滴、雪花、樱花、星星、枫叶、气泡和萤火虫。没有照片时只播放粒子；上传照片后，照片会从粒子运动方向出现，在中央放大展示，再离场。

可以设置粒子数量、运动方向、运动速度、照片展示时长和是否循环。方向包括从上到下、从下到上、从左到右、从右到左、两种斜向和随机方向。

### 嵌入网页

选择“嵌入网页”模板，输入完整的 `https://` 地址即可。目标网页必须允许 iframe 加载，否则浏览器会根据目标网站的安全策略拒绝显示。

## 保存和共享

使用导出工程功能生成 `.pptist` 文件。该文件会保存页面、动画、HTML/CSS/JavaScript 代码、模板属性和图片数据。接收者在 PPTist 中导入 `.pptist` 文件即可继续使用。

## PPTX 导入

PPTX 导入会转换常见页面元素和动画时序。由于 PowerPoint 的高级路径动画、复杂组合和厂商扩展没有统一的 Web 表达方式，导入后仍建议在动画面板中检查关键页面。

name: Deploy PowerPoint task pane

on:
  push:
    branches: [main]
    paths:
      - 'office-addin/**'
      - '.github/workflows/deploy-office-addin-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: office-addin-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: office-addin
      - id: deployment
        uses: actions/deploy-pages@v4
